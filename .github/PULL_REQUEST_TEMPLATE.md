<!--
Thanks for sending this.

Two things worth knowing before you spend time on it:

  * Sign off your commits (`git commit -s`). CI checks it; if you forget,
    `git rebase --signoff origin/main` fixes the whole branch at once.
  * Development happens in a private repository and lands here as export
    snapshots, so your patch is applied there with your authorship preserved
    and arrives here in the next release. See CONTRIBUTING.md.
-->

## What this changes

## Why

<!-- The diff shows what changed. This section is for what problem it solves
     and what you considered. -->

## How you know it works

<!-- A test that would fail without the change is the strongest form of this.
     A test that passes before and after proves nothing. -->
